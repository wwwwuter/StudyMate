Flask==3.0.3
Flask-CORS==4.0.1
openai==2.48.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
PyMySQL==1.1.1
cryptography==42.0.8
PyJWT==2.8.0
Werkzeug==3.0.3
python-dotenv==1.0.1
requests==2.32.3
APScheduler==3.10.4
openpyxl==3.1.5
pypdf>=3.0.0
pdfminer.six==20231228
# langchain 系列已移除：当前代码未 import，且其 0.2.x 硬锁 numpy<2.0，
# 与 Python 3.13 + 现代科学栈（numpy>=2）冲突，会装回崩溃的 MINGW 实验构建。
# 若未来实现 RAG，再按需加回新版（>=0.3，支持 numpy2）。
sentence-transformers==2.7.0
faiss-cpu>=1.9.0
numpy>=2.0  # Python 3.13 需 numpy>=2（官方 cp313 轮子；<2 会装回崩溃的 MINGW 实验构建）
gunicorn==22.0.0
# ---- 桌面端打包（Phase 9.5：后端随 exe 一键启动）----
waitress==3.0.2      # desktop_run.py 生产 WSGI 服务器
pyinstaller==6.21.0  # 冻结后端为 studymate-backend.exe（仅构建机需要）
