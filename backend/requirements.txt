# StudyMate 后端生产依赖（Web 化部署）
# 安装：pip install -r requirements.txt

# Web 框架
Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-CORS==5.0.0
Flask-Limiter==4.1.1
python-dotenv==1.0.1

# 数据库（MySQL）
PyMySQL==1.1.1
# 若用 PostgreSQL 则改为：psycopg2-binary

# 密码/会话
cryptography==49.0.0

# 调度（提醒扫掠）
APScheduler==3.10.4

# AI / RAG
openai==2.48.0
sentence-transformers==5.6.1
faiss-cpu==1.14.3
numpy==1.26.4
# RAG 首次运行会自动下载向量模型权重（shibing624/text2vec-base-chinese），
# 服务器需能访问 HuggingFace，或提前缓存到本地。

# 文档解析
python-docx==1.2.0
pdfminer.six==20231228

# WSGI 服务器（Docker 内用 gunicorn 启动）
gunicorn==23.0.0
# 备选：waitress==3.0.2
