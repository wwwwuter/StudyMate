# StudyMate 提示词模板（仅保留计划解析所需的三个）
#
# 约定：变量统一用哨兵占位符 <<<VAR>>>，渲染时用 PromptManager.render 替换，
# 避免使用 .format() 导致 JSON 大括号 / 未转义花括号触发 KeyError。
# 这些内置常量作为兜底（当 prompts/ 目录下缺少对应 .txt 文件时使用）。

DOCX_TASK_EXTRACT_PROMPT = """请从以下从 Word 学习计划文档中提取的纯文本中，识别所有「学习任务」，并提取为结构化 JSON。

文档可能是以下任意格式，请自动识别并适配：
- 打卡表：一行内多个 [ ] 科目：内容，或带周次（周一/周二/7月最后一周）
- 普通列表：每行一个 科目：内容 或 科目|内容|优先级
- 表格：多列数据（科目/内容/时间/优先级）
- 自由文本：自然语言描述（如"今天学数学第一章，明天做英语阅读"）
- 混合：以上多种混合

提取规则：
1. 识别所有"学习任务"（有明确科目+内容的条目），放入 daily_tasks
2. 识别"作息安排"（固定时间块，如 08:30-11:30【数学】：...）→ 放入 schedule_template
3. 识别"周目标/阶段目标"（如"8月第一周：高数强化"）→ 放入 weekly_goals
4. 忽略标题、说明、使用建议、（AI生成）等非任务内容
5. 若一行内有多个任务（如打卡表的多个 [ ] 段），必须拆成独立任务，不要合并

输出格式（必须严格遵循）：
{
  "schedule_template": [
    {"time": "07:30-08:30", "label": "晨读", "content": "英语单词100-150词 + 数学公式默写"}
  ],
  "weekly_goals": [
    {"week": "7月最后一周", "theme": "基础扫尾与强化启动"}
  ],
  "daily_tasks": [
    {
      "date": null,
      "date_note": "相对周次，需用户指定起始日期",
      "subject": "数学",
      "content": "概率论：二维随机变量（基础课+课后题）",
      "start_time": null,
      "end_time": null,
      "is_schedule": false,
      "status": "pending",
      "confidence": 0.95,
      "reason": "原文周一行的第一个 [ ] 段"
    }
  ]
}

字段说明：
- schedule_template: 作息安排（如有），每项含 time/label/content
- weekly_goals: 周目标（如有），每项含 week/theme
- daily_tasks: 每日任务（必须有），每项含：
  - date: YYYY-MM-DD 或 null（相对日期时）
  - date_note: 相对日期说明（如"周一，需用户指定起始日期"）
  - subject: 科目，尽量用标准名：数学/英语/政治/408（高数/线代/概率→数学，数据结构/计组/操作系统/计网→408）
  - content: 任务内容（简要，剥离 [ ]/【科目】 等标记）
  - start_time/end_time: HH:MM 或 null
  - is_schedule: false（daily_tasks 里都是 false，作息在 schedule_template）
  - status: 固定 "pending"
  - confidence: 0-1 自评准确度
  - reason: 提取依据（如"原文第3行"）

重要：
- 不要猜测具体日期；原文有绝对日期（如 2026-07-20 / 7月20日）才填 date
- 若某类信息不存在（如无作息表），对应字段输出空数组 []
- 若文本中没有明确任务，daily_tasks 输出 []

待解析文本：
<<<TEXT>>>
"""


PDF_TASK_EXTRACT_PROMPT = """请从以下从 PDF 学习计划文档中提取的纯文本中，识别所有「学习任务」，并提取为结构化 JSON。

提取字段（每个任务一项）：
- date: 任务日期，格式 YYYY-MM-DD。原文若写「7月20日」「2026/7/20」「2026.7.20」请归一化为 2026-07-20；无法推断年份时默认 2026。
  **重要**：若日期是相对表述（如「今天/明天/下周/本周」）或年份/月份缺失到无法可靠推断，请设 date 为 null，并在 date_note 中说明歧义（例如「相对日期，需用户确认」）。不要猜测具体日期。
- date_note: 当 date 为 null 或存在歧义时的说明；日期明确时设为 null。
- subject: 科目，尽量使用标准名：数学、英语、政治、408（如「高数/线代/概率」→数学，「数据结构/计组/操作系统/计网」→408）。
- content: 任务内容（简要，如「极限专题训练」）。
- start_time: 开始时间 HH:MM，无则 null。
- end_time: 结束时间 HH:MM，无则 null。
- status: 固定为 "pending"。
- confidence: 你对本次提取准确度的自评，0 到 1 之间的小数（0.9 表示很有把握，0.5 表示不确定）。
- reason: 简短说明该条任务为何被如此提取（如「原文第二行明确列出」），便于用户复核。

输出要求：
1. 只输出一个 JSON 对象，不要任何解释或 markdown 代码块。
2. 结构：{"tasks": [ {...}, ... ]}
3. 若文本中没有明确任务，输出 {"tasks": []}

待解析文本：
<<<TEXT>>>
"""


PLAN_VISION_PROMPT = """你是一个学习计划解析助手。用户上传了一张「学习计划表 / 计划清单 / 作息安排」的图片（可能是手机截图、拍摄的纸质计划、或表格照片）。

请仔细识别图片中的全部学习/复习计划条目，按时间把它们结构化。

要求：
1. 逐条识别，不要遗漏；若图中无明显时间，则按出现顺序编号。
2. 每条输出一个 JSON 对象，字段：
   - "date": 日期，格式 "YYYY-MM-DD"。若图中写的是「今天/周一/3月5日」等相对或模糊表述，请结合当前日期合理推算为绝对日期；无法确定则填 null。
   - "subject": 科目/分类，中文简短词，如 数学/英语/政治/408/专业课。
   - "content": 该时段的具体学习内容（一句话，保留原意）。
   - "start_time": 开始时间 "HH:MM"，无法确定填 null。
   - "end_time": 结束时间 "HH:MM"，无法确定填 null。
3. 严格只输出一个 JSON 数组，不要任何解释、不要 markdown 代码块标记。

示例输出：
[{"date":"2026-08-02","subject":"数学","content":"高数 第三章习题","start_time":"09:00","end_time":"10:30"},{"date":"2026-08-02","subject":"英语","content":"背单词+阅读理解","start_time":"14:00","end_time":"15:30"}]
"""
