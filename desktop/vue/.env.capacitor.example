# StudyMate 移动端（Capacitor Android）构建配置示例
# 复制为 .env.capacitor 并按需修改，然后按 README「移动端构建」流程构建：
#   copy .env.capacitor.example .env.capacitor
#   (修改 VITE_API_BASE 为你自己的后端地址)
#   npm run build:capacitor

# ---- 后端 API 地址（关键）----
# request.ts 已支持三路回退：VITE_API_BASE > electronAPI > /api 相对路径
# 移动端（无 electronAPI）必须显式注入。两种形态：

# 形态一：局域网（电脑 + 平板同一 Wi-Fi，测试用）
# 指向电脑的网页版端口（4173，vite preview 会把 /api 代理到后端 5088，后端无需对外暴露）
# 注意：把 192.168.x.x 换成你电脑的实际局域网 IP（ipconfig 查看）
VITE_API_BASE=http://192.168.164.1:4173

# 形态二：云端正式部署（上云后启用）
# 指向部署了 Flask + MySQL 的服务器域名（nginx 反代 /api）
# VITE_API_BASE=https://api.yourdomain.com

# ---- 其他（可选）----
# VITE_APP_NAME=StudyMate
