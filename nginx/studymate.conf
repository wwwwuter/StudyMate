# StudyMate 前端静态站 + 后端 API 反代
# 静态站由 `desktop/vue/dist` 挂载到 /usr/share/nginx/html
server {
    listen 80;
    server_name yourdomain.com;  # 改成你的域名

    # 上传资料可能需要较大 body
    client_max_body_size 20m;

    # 前端 SPA：所有非 /api 路径回退到 index.html
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 反代
    location /api/ {
        proxy_pass http://backend:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;  # AI 解析较慢，放宽超时
    }

    # 健康检查
    location /api/health {
        proxy_pass http://backend:5000;
    }
}

# HTTPS 示例（启用时取消注释，并把上面 server 改为 443 + 80 跳转）
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     client_max_body_size 20m;
#     location / { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
#     location /api/ { proxy_pass http://backend:5000; ... }
# }
# server {
#     listen 80; server_name yourdomain.com;
#     return 301 https://$host$request_uri;
# }
